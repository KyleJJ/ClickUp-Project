import { FormatPlayerPipe } from './format-player.pipe';

describe('FormatPlayerPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatPlayerPipe();
    expect(pipe).toBeTruthy();
  });
});
